# 更新记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的简化格式；版本号遵循语义化版本的意图。

## [Unreleased]
- 修复切换账号后旧封面排队任务与未完成 Promise 继续占用新账号加载链路；取消旧请求并保留 URL 磁盘缓存，实际并发仍不超过 8。

发布后优化已进入 `main`（2026-09-10）。尚未切新的 GitHub Release；从 `main` 的 `src/` 导入即可获得这些改动。

### 修复
- 自动翻页按画廊总页数继续，不再停在首批 20 页预览窗口；手动点翻页不会打断自动播放。
- 阅读器启动自动翻页后收起底部控制栏，避免挡住图片；点画面仍可唤出暂停或关闭。
- ExHentai 封面域名 s.exhentai.org 超时不再立刻重试，也不再占满全部 8 路；该主机最多 2 路，失败显示“重试”。host-only Ex Cookie 会发给该封面子域。
- 收藏分类、搜索及翻页离开旧列表时，按封面消费者取消无人使用的排队/在途任务；共享图片和磁盘缓存保留，取消后不再后台重试。重选当前分类不会中止加载。

- 未收藏画廊不再显示默认第一个收藏夹。
- 无效详情/下载地址会在请求前拒绝，不再抛出底层 Request TypeError。
- 暂停下载不会覆盖已完成或失败的任务状态。
- 系统图库保存尚未结束时，禁止再次点击重试。
- Cookie 草稿不再写入 iCloud Drive；导入成功后清理本机 GM 草稿。

### 变更

- 画廊卡片显示语言标签，不额外加汉化筛选。
- 发现页搜索按钮改为与 AI 搜索并排的气泡按钮。
- iPad 画廊详情改为盖在分栏内容上的 overlay；点侧栏会关掉这层，直接看到发现/书库/设置。
- 从画廊里的标签/上传者结果再点画廊，会在当前栈再推一层，返回回到上一页。
- 浏览画廊时自动隐藏 iPad 侧栏。
- 书库/收藏封面改为先加载前 6 张，其余进屏再下，避免 50 张一起转圈。
- 收藏封面超时不再显示黄感叹号；失败自动重试一次后用灰色占位。GIF 封面可显示。
- 图片缓存改到系统临时目录，不再写入 iCloud 脚本目录；所有图片（含 ehgt）统一走 8 路队列；同一封面 URL 跨首页/详情/收藏复用，不再因 stage/referer 重复下载。
- 画廊评分改为 5 个爱心点选，支持半心，并继续显示均分。
- 发现页改为单一标题与按内容高度的搜索卡。
- 连续阅读在页面出现后再解析图片；「查看全部」使用预览窗口，避免超长画廊一次渲染全部缩略图。
- 下载进度刷新改为节流；图片内存缓存设有上限。
- 画廊详情资源区可使用系统分享。

### 安全

- 诊断日志进一步脱敏，不再输出堆栈与本机路径。
- AI 搜索输入会剥离明显的凭据字符串。
- Issue 表单改为 YAML，并要求提交前确认已脱敏。
- 公开发布的 `src/` 不再包含内部 DEV 提交/引导脚本。

## [1.1.0] - 2026-09-03

首个公开发布版本。

### 新增

- 原生画廊浏览、关键词/标签/高级筛选搜索，以及保存搜索条件。
- 画廊详情、预览页面、在线单页/连续阅读、缩放、阅读进度与图片缓存。
- 本地书签、历史记录、继续阅读、服务端收藏夹与收藏分类管理。
- 完整画廊下载与按页选择的部分下载；可保存至离线书库、系统图库或两者。
- 可暂停、继续、重试并在启动后恢复的前台下载；已保存内容可在 Offline Reader 阅读。
- E-Hentai / ExHentai 会话导入与验证，以及 Safari Cookie 助手。
- 可选的 Scripting Assistant 搜索与推荐。

### 可靠性与隐私

- 登录、站点切换与异步请求按当前账户/站点会话隔离，避免过期结果覆盖当前状态。
- 下载任务区分完整与部分下载的完成条件，并恢复中断的本地文件操作。
- 保存至系统图库遇到中断时，会提示用户确认后才重试，以避免静默重复保存。
- 不包含项目运营方的分析、遥测或自建后端；新增公开的隐私说明和敏感信息反馈指引。

### 已知限制

- 下载依赖 Scripting 在前台运行，不提供无限制后台下载。
- ExHentai 访问取决于真实有效的 ExHentai 会话和账户权限。
- 本版本不是 EhViewer 或其他 Android 客户端的完整功能复刻。

[unreleased]: ../../compare/v1.1.0...main
[1.1.0]: ../../releases/tag/v1.1.0
